import type { RequestDetail, RequestListItem } from '@clawreview/shared';

export function presentRequestListItem(request: RequestListItem): RequestListItem {
  return request;
}

export function presentRequestDetail(request: RequestDetail): RequestDetail {
  return request;
}
